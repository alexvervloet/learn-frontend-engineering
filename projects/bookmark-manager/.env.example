# Everything prefixed VITE_ is inlined into the bundle. See learning/production.
VITE_API_URL=/api
VITE_COMMIT_SHA=local

# Point the dev proxy at a running Practice-Backends bookmark-manager.
# With no backend, the MSW Service Worker answers instead.
VITE_API_PROXY=http://localhost:8000
