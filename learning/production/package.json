{
  "name": "production",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Env config, CSP and XSS, auth in the browser, error tracking, feature flags, i18n",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "e2e": "playwright test",
    "e2e:install": "playwright install chromium"
  },
  "dependencies": {
    "@lab/lesson-shell": "*",
    "dompurify": "^3.4.15",
    "i18next": "^26.4.2",
    "react-i18next": "^17.0.14"
  }
}
