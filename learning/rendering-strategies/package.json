{
  "name": "rendering-strategies",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "CSR, SSR, streaming, SSG and ISR, against React's own APIs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lab/lesson-shell": "*"
  }
}
